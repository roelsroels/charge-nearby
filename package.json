{
  "name": "charge-nearby",
  "version": "1.0.6",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check html/app.js && node --check server.mjs && node --check lib/enbw.mjs",
    "test": "node --test tests/*.mjs"
  }
}
